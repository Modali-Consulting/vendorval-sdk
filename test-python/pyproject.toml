[project]
name = "vendorval-test"
version = "0.0.0"
requires-python = ">=3.9"
dependencies = [
    "vendorval",
    "python-dotenv>=1.0",
]

[tool.uv.sources]
vendorval = { path = "../packages/python", editable = true }
